기존 인스턴스를 복제하여 새로운 인스턴스를 만듬
보통 기존의 객체를 이용해서 새로운 객체를 만들때 사용함
특히 시간이 오래걸리는 경우(db 조회, http통신을 하여 객체를 생성해야한다던가), 매번 해당 작업을 하는 것이 아니라 복제하여 사용함

장점
1. 복잡한 객체를 만드는 과정을 숨길 수 있다.
2. 새 인스턴스를 만드는 것 보다 비용(시간 또는 메모리)적인 면에서 효율적일 수 있다.
3. 추상적인 타입을 리턴 할 수 있다.

단점
복잡한 객체를 만드는 과정 자체가 복잡할 수 있다. (특히, 순환 참조가 있는 경우)